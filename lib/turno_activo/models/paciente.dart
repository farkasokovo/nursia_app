// ── MODELO PACIENTE ────────────────────────────────────────────────────────
// Clase escalable: agrega campos aquí cuando necesites más datos.
class Paciente {
  final String nombre;
  // Futuros campos:
  // final int? edad;
  // final String? diagnostico;
  // final String? cama;

  const Paciente({required this.nombre});
}
